export interface User {
  id: string;
  name: string;
  email: string;
  phone?: string;
  walletAddress?: string;
  profileImage?: string;
  coverImage?: string;
  avatarUrl?: string;
  bio?: string;
  createdAt: string;
  updatedAt: string;
} 